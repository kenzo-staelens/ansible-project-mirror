---
# tasks file for roles/common
- name: Update and upgrade software DNF
  ansible.builtin.debug:
    msg: "test {{ ansible_package_manager }}"
  changed_when: ansible_package_manager == 'dnf'
- name: Update and upgrade software APT
  ansible.builtin.debug:
    msg: "test {{ ansible_package_manager }}"
  changed_when: ansible_package_manager == 'apt'
#   ansible.builtin.dnf:
#     name: "*"
#     update_cache: true
#     state: latest
# - name: Update and upgrade software APT
#   ansible.builtin.apt:
#     name: "*"
#     update_cache: true
#     state: latest
